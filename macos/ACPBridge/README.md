# ACP Bridge (macOS shell)

The SwiftUI menu-bar app shell will be added in **Task 7**.

Open or create the Xcode project here in Task 7, then wire `scripts/build-app.sh` to copy the built `ACPBridge` binary into `dist/ACP Bridge.app/Contents/MacOS/`.
